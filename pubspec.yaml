name: bootstrap
description: Core entities, components, interfaces, repositories, events etc.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  event_bus: ^2.0.1

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  very_good_analysis: ^10.0.0
  mocktail: ^1.0.4
  